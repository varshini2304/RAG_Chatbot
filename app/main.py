"""Application package entry placeholder.

The root-level `main.py` owns the current Streamlit application shell. This
module is reserved for future application factory or orchestration code if the
UI grows beyond a single entry point.
"""

# TODO: Add application factory wiring in a future SDLC step if needed.
